# Supabase project settings (Project Settings -> API). Only the anon/public
# key is needed — order and subscriber tables grant it INSERT only via row
# level security, so it can never read back what it writes. See
# supabase/schema.sql for the policies and lib/supabase.ts for how it's used.
SUPABASE_URL=
SUPABASE_ANON_KEY=
